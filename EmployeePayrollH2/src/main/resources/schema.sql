CREATE TABLE [IF NOT EXISTS] employee_payroll (id INTEGER unsigned NOT NULL AUTO_INCREMENT,name VARCHAR(150) NOT NULL,gender varchar(20) not null,salary Double NOT NULL,start DATE NOT NULL,PRIMARY KEY (id))